ao fazer o git clone primeiro crie um ambiente virtual na sua maquina com o comando python -m venv venv depois de criado entre na pasta venv depois na pas script depois digite ./activate assim vocÊ estará ativando seu ambiente virtual , depois volte para a pasta principal do prjeto onde esta os arquivo manage.py ,  depois disso instale as bibliotecas do requiremments.txt com o comando pip install requirements.txt  após instadas as bibliotecas voce pode dar o comando python manage.py runserver para rodar o servidor do django para acessar a pagina do projeto
